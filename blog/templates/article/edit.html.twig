{% extends 'base.html.twig' %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('css/article/article-form.css') }}">
{% endblock %}

{% block title %}Edit Article{% endblock %}

{% block content %}
    <div class="top-article-new">
        <h1>Editer un article</h1>
        <a class="btn" href="{{ path('articles') }}">Retour aux articles</a>
    </div>
    {{ include('article/_form.html.twig', {'button_label': 'Editer'}) }}

    {% if constant('App\\Entity\\User::ROLE_ADMIN')[0] in app.user.roles %}
        {{ include('article/_delete_form.html.twig') }}
    {% endif %}
{% endblock %}
